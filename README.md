Python: 3.9+

Hardware: NVIDIA GPU (recommended)

Framework: CUDA-enabled PyTorch (install the build that matches your CUDA version)

To train the model, run the training script; to generate outputs, run the inference script. Update all file paths (datasets, checkpoints, and output directories) to match your environment.